# Cloudflare RPlace Clone

Testing cloudflare stack with a r/place clone.

## Log

```
bun create cloudflare@latest cloudflare-rplace-backend
bun create cloudflare@latest cloudflare-rplace-frontend

bun init -y

bun add '@cloudflare-rplace/shared@workspace:*'
```